{% extends 'base.html.twig' %}

{% block body %}
    <h3>{{ package.name }}</h3>

    {% for reference in package.references %}
        <a href="{{ path('project_view', {uuid: reference.project.uuid}) }}">{{ reference.projectName }}</a> - {{ reference.version }}
        <br>
    {% endfor %}
{% endblock %}

{% block javascripts %}

{% endblock %}

