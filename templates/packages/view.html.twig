{% extends 'base.html.twig' %}

{% block body %}
    <h3>{{ package.name }}</h3>

    {% for reference in package.references %}
        {{ reference.projectName }} - {{ reference.version }}<br>
    {% endfor %}
{% endblock %}

{% block javascripts %}

{% endblock %}

